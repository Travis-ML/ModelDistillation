# Distillation of Generative Models — course dependencies.
#
# Versions below are the ones the Tier 1 labs and every Tier 2 Part A cell were
# actually executed and asserted against during the build. Minimums are pinned
# because the labs introspect trl.experimental configs at runtime (the APIs
# there change between minor versions — the labs will tell you if drift breaks
# an assumption, but start from a known-good floor).
#
# torch: the vendor container's bundled build WINS. On arm64/sm_121 boxes the
# GPU-enabled torch comes from the container (NGC 25.10/25.11-era images ship
# 2.9/2.10 vendor builds; community rebuilds like scitrera's match that), and
# upstream pip wheels may lack sm_121 kernels entirely — never pip-upgrade
# torch inside the container to satisfy this file. The floor below is set
# BELOW current Spark containers on purpose: (a) the course only uses
# long-stable torch APIs, and (b) vendor builds carry pre-release version
# strings (e.g. 2.10.0a0+...) that pip treats as LESS than 2.10, so a tight
# floor would spuriously reject a working container.

torch>=2.8
transformers>=5.14
trl>=1.9.2
datasets>=5.0
accelerate>=1.14
numpy>=2.0
matplotlib>=3.10
requests>=2.32

# Notebook execution
jupyter
nbformat>=5.10
nbclient>=0.11
ipykernel>=7.0

# --- Optional, per lab ---
# Lab 08 (teacher server): vLLM is NOT pip-installed here on purpose — on
# arm64/sm_121 use a known-good prebuilt container for serving (see the lab
# and the README's arm64 notes). The training-side client only needs requests.
# vllm
#
# Lab 11 (benchmark pass):
# lm-eval>=0.4
